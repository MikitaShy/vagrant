pipeline 
{

    agent any

    stages 
    {
        stage('Connection test') 
        {
            steps 
            { 
                    sh '''#!/bin/bash
                    echo "bash script is running" 
                    ansible-playbook /root/Ansible/playbooks/playbook1.yml -i
                    echo "playbook1 is running" 
                    '''
            }
        }
    }
}
